# Database Configuration
DB_NAME=app_stats_db
DB_USER=app_stats_user
DB_PASSWORD=your_password_here
DB_HOST=localhost

# Elasticsearch Configuration
ES_HOST=http://your.elasticsearch.host:443
ES_USER=elastic_user
ES_PASSWORD=elastic_password

# Staging Area Configuration
STAGING_AREA_PATH=/path/to/your/staging/area

# Optional Configurations
CONFIG_FILE=config.yaml  # Relative to the script directory

# Note: Replace all values with your actual configuration
# Do not commit the actual .env file to version control